<div class="container">
  <h1>All my lists</h1>
  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list)  %>
  <% end %>
  <h1>New list</h1>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>
</div>
